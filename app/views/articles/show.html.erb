<div class="main posts-show">
  <div class="container">
    <div class="opacity">
    <div class="posts-show-item">
      <h1><%= @article.title %></h1>

      <p>
        <%= @article.content %>
        <%= @article.day %>
        </p>

        <div class="post-time">
        <%= @article.created_at %>
        </div>

        <div class="post-menus">
          <%= link_to("編集", "/articles/#{@article.id}/edit") %>
          <%= link_to("削除", "/articles/#{@article.id}/destroy", {method: "post"}) %>
          <%= link_to("home","/") %>
          </div>
          </div>
          </div>
          </div>
          </div>
